kind = "web"
previewPath = "/"
title = "ClientHunter CRM"
version = "1.0.0"
id = "artifacts/clienthunter-crm"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21118

[services.development]
run = "pnpm --filter @workspace/clienthunter-crm run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/clienthunter-crm", "run", "build" ]
publicDir = "artifacts/clienthunter-crm/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21118"
BASE_PATH = "/"
